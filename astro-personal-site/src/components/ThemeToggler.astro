---
// This section runs the server. // window.localStorage.getItem("value"); // Error, not run on client
---
<!--<button type="button">Click Me!</button>--><!-- This button will be used instead of the svg-button, if we use a global selector (document.querySelector("button")) 
We're fixing this by using a data attribute-->
<button aria-label="Theme Toggler" data-theme-toggler>
    <svg width="25px" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
    <path
      class="sun"
      fill-rule="evenodd"
      d="M12 17.5a5.5 5.5 0 1 0 0-11 5.5 5.5 0 0 0 0 11zm0 1.5a7 7 0 1 0 0-14 7 7 0 0 0 0 14zm12-7a.8.8 0 0 1-.8.8h-2.4a.8.8 0 0 1 0-1.6h2.4a.8.8 0 0 1 .8.8zM4 12a.8.8 0 0 1-.8.8H.8a.8.8 0 0 1 0-1.6h2.5a.8.8 0 0 1 .8.8zm16.5-8.5a.8.8 0 0 1 0 1l-1.8 1.8a.8.8 0 0 1-1-1l1.7-1.8a.8.8 0 0 1 1 0zM6.3 17.7a.8.8 0 0 1 0 1l-1.7 1.8a.8.8 0 1 1-1-1l1.7-1.8a.8.8 0 0 1 1 0zM12 0a.8.8 0 0 1 .8.8v2.5a.8.8 0 0 1-1.6 0V.8A.8.8 0 0 1 12 0zm0 20a.8.8 0 0 1 .8.8v2.4a.8.8 0 0 1-1.6 0v-2.4a.8.8 0 0 1 .8-.8zM3.5 3.5a.8.8 0 0 1 1 0l1.8 1.8a.8.8 0 1 1-1 1L3.5 4.6a.8.8 0 0 1 0-1zm14.2 14.2a.8.8 0 0 1 1 0l1.8 1.7a.8.8 0 0 1-1 1l-1.8-1.7a.8.8 0 0 1 0-1z"
    ></path>
    <path
      class="moon"
      fill-rule="evenodd"
      d="M16.5 6A10.5 10.5 0 0 1 4.7 16.4 8.5 8.5 0 1 0 16.4 4.7l.1 1.3zm-1.7-2a9 9 0 0 1 .2 2 9 9 0 0 1-11 8.8 9.4 9.4 0 0 1-.8-.3c-.4 0-.8.3-.7.7a10 10 0 0 0 .3.8 10 10 0 0 0 9.2 6 10 10 0 0 0 4-19.2 9.7 9.7 0 0 0-.9-.3c-.3-.1-.7.3-.6.7a9 9 0 0 1 .3.8z"
    ></path>
  </svg>
</button>

<style>
    button {
        cursor: pointer;
        border-radius: 10px;
        border: 0;
        padding: 5px 10px;
        transition: all 0.2 ease-in-out;
    }

    button:hover {
        transform: scale(0.9);
    }

    button:active {
        transform: scale(1);
    }

    .sun {
        fill: transparent;
    }

    :global(.dark) .sun { /* if a parent element got a dark class */
        fill: black;
    }

    :global(.dark) .moon {
        fill: transparent;
    }
    /* 
    CSS transitions allows you to change property values smoothly, over a given duration.

    The transition-timing-function property can have the following values:

    ease - specifies a transition effect with a slow start, then fast, then end slowly (this is default)
    linear - specifies a transition effect with the same speed from start to end
    ease-in - specifies a transition effect with a slow start
    ease-out - specifies a transition effect with a slow end
    ease-in-out - specifies a transition effect with a slow start and end
    cubic-bezier(n,n,n,n) - lets you define your own values in a cubic-bezier function
    The following example shows some of the different speed curves that can be used:*/
</style>

<script is:inline>
    // Astro is not going to process inline scripts
    // Don't import NPM
    // Don't use TypeScripts
    // We want this script to be used multiple times,
    // since we wan't our page to start in the chosen theme
</script>

<script> // This section supports TypeScript. This section runs on the client.
    import { titleCase } from "title-case";
    // Some of the functionality for the theme are copied or moved to a inline script section
    // in the the Main.astro layout file, in order to enable the vars for the dark theme 
    // before the browser starts drawing the page
    const DARK_THEME_CLASS = "dark";
    const COLOUR_MODE = "COLOUR_MODE";
    const LIGHT_THEME = "LIGHT";
    const DARK_THEME = "DARK";

    // Grab the toggle
    const toggle = document.querySelector("[data-theme-toggler]"); // returns first Element that matches, so use a data-attribute insteaod of a global selector
    const rootEl = document.documentElement;    

    if(toggle) {
        toggle.addEventListener("click", () => {
            rootEl.classList.toggle(DARK_THEME_CLASS);

            const colourMode = rootEl.classList.contains(DARK_THEME_CLASS)
                ? DARK_THEME
                : LIGHT_THEME;

            window.localStorage.setItem(COLOUR_MODE, colourMode);
        });
    }
</script>